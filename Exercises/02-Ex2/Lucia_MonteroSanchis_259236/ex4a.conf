# Simple config-file

server {
  listen 443 ssl;
  listen [::]:443;
  ssl on;
  ssl_certificate /etc/nginx/conf.d/n-ss.crt;
  ssl_certificate_key /etc/nginx/conf.d/n-ss.key;

  location / {
    root /www;
  }
}
